version: '3.5'
services:
  # certbot:
  #   image: certbot/certbot
  #   container_name: certbot
  #   volumes:
  #     - certbot-etc:/etc/letsencrypt
  #     - certbot-var:/var/lib/letsencrypt
  #     - app:/var/www/html
  #   command: certonly --standalone --preferred-challenges http -d wss.simpletrader.com
  socket-gateway-1:
    build:
      context: ./
      dockerfile: Dockerfile
    hostname: socket-gateway-1
    container_name: socket-gateway-1
    restart: unless-stopped
    tty: true
    env_file:
      - .one.env
    volumes:
      - app:/var/www/html
    command:
      - yarn
      - start-dev
  socket-gateway-2:
    build:
      context: ./
      dockerfile: Dockerfile
    hostname: socket-gateway-2
    container_name: socket-gateway-2
    restart: unless-stopped
    tty: true
    env_file:
      - .two.env
    volumes:
      - app:/var/www/html
    command:
      - yarn
      - start-dev
  socket-gateway-proxy:
    image: nginx:alpine
    container_name: socket-gateway-proxy
    restart: unless-stopped
    depends_on:
      - socket-gateway-1
      - socket-gateway-2
    ports:
      - 8080:8080
      - 8443:8443
    tty: true
    volumes:
      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./docker/nginx/certs/:/etc/nginx/ssl
      # - certbot-etc:/etc/letsencrypt
      # - certbot-var:/var/lib/letsencrypt

# secrets:
  # ssl_cert:
  #   file: ./nginx/certs/fullchain.pem
  # ssl_key:
  #   file: ./nginx/certs/privkey.pem
  # ssl_dhparam:
  #   file: ./nginx/certs/dhparam.pem

volumes:
  # certbot-etc:
  # certbot-var:
  app:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ./

networks:
  default:
    name: 'simple-trader'
    external: true